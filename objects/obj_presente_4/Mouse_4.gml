/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor

if (global.presente_4 = true) or (global.pontos < valor)
{
	exit;
}
else if (global.pontos >= valor)
{
	global.pontos -= valor;
	global.presente_4 = true;
	image_blend = c_gray;
}